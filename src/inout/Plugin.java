package inout;

import javax.script.ScriptEngine;
import javax.script.ScriptEngineManager;
import javax.script.ScriptException;

public class Plugin {
	
	/**
	 * Load the plugins that are defined in a manifest file; sort of like 
	 * keymap.xml. Not only does it load them, it pre-compiles them too.
	 */
	public static void loadPlugins() {
		
	}
	
	/**
	 * Run the plugin with the designated string.
	 * 
	 * @param plugin
	 * @param args
	 */
	public static void runPlugin(String plugin, String[] args) {
		
	}
	
	

}
